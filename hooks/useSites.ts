/**
 * useSites — re-exports useSiteResolver for backwards compatibility.
 *
 * @deprecated Import `useSiteResolver` from `@/hooks/useSiteResolver` directly.
 * This re-export exists so existing callers continue to work without changes.
 */

export { useSiteResolver as useSites } from "@/hooks/useSiteResolver";
